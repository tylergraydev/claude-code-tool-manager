import { describe, it, expect } from 'vitest';

describe('Component Module Exports', () => {
	it.skip('should export SearchBar component - skipping due to CI flakiness', async () => {
		const component = await import('$lib/components/shared/SearchBar.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export ConfirmDialog component', async () => {
		const component = await import('$lib/components/shared/ConfirmDialog.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export EnvEditor component', async () => {
		const component = await import('$lib/components/shared/EnvEditor.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export Header component', async () => {
		const component = await import('$lib/components/layout/Header.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export Sidebar component', async () => {
		const component = await import('$lib/components/layout/Sidebar.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export CommandCard component', async () => {
		const component = await import('$lib/components/commands/CommandCard.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export CommandForm component', async () => {
		const component = await import('$lib/components/commands/CommandForm.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export ProjectCard component', async () => {
		const component = await import('$lib/components/projects/ProjectCard.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export McpCard component', async () => {
		const component = await import('$lib/components/mcp/McpCard.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export McpForm component', async () => {
		const component = await import('$lib/components/mcp/McpForm.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export HookCard component', async () => {
		const component = await import('$lib/components/hooks/HookCard.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export HookForm component', async () => {
		const component = await import('$lib/components/hooks/HookForm.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export SkillCard component', async () => {
		const component = await import('$lib/components/skills/SkillCard.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export SkillForm component', async () => {
		const component = await import('$lib/components/skills/SkillForm.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export SubAgentCard component', async () => {
		const component = await import('$lib/components/subagents/SubAgentCard.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export SubAgentForm component', async () => {
		const component = await import('$lib/components/subagents/SubAgentForm.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export SoundBrowser component', async () => {
		const component = await import('$lib/components/sounds/SoundBrowser.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export SoundPicker component', async () => {
		const component = await import('$lib/components/sounds/SoundPicker.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});

	it('should export ClaudeJsonView component', async () => {
		const component = await import('$lib/components/claude/ClaudeJsonView.svelte');
		expect(component).toBeDefined();
		expect((component as any).default).toBeDefined();
	});
});
